package com.orbital2019.plannerplusplus

import android.arch.lifecycle.LiveData

class EventProcessor {
    // lateinit var AllData = EventsDao.allEvents
    
    fun ResolveEvent(e: Event, a: Action) {
        // add new entry to log
        // check if event has any triggers
        // run triggers
        // move event to deleted list
    }
    
    fun 
    
    
}